<html>
<head>
</head>
<body>
  <a href="documenturl.php?name=<script>alert('XSS')</script>"> DocumentURL.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

  <a href="documenturi.php?name=<script>alert('XSS')</script>">DocumentURI.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM. 
<br><br>

<a href="documenturlunencoded.php?name=<script>alert('XSS')</script>">DocumentURLUnencoded.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM. 
<br><br>

<a href="documentbaseuri.php?name=<script>alert('XSS')</script>">DocumentbaseURI.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

<a href="location.php?name=<script>alert('XSS')</script>">Location.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

<a href="locationhref.php?name=<script>alert('XSS')</script>">LocationHref.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

<a href="locationsearch.php?name=<script>alert('XSS')</script>">LocationSearch.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM. 
<br><br>

<a href="locationhash.php?name=<script>alert('XSS')</script>">LocationHash.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

<a href="locationpathname.php?name=<script>alert('XSS')</script>">LocationPathname.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

<a href="documentcookie.php?name=<script>alert('XSS')</script>">DocumentCookie.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

<a href="documentreferrer.php?name=<script>alert('XSS')</script>">DocumentReferrer.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

<a href="windowname.php?name=<script>alert('XSS')</script>">WindowName.php</a>This DOM based XSS found on IE 9 or later. As range.CreateContextualelement support from  IE 9 or later. The new range is going to define and payload is provide through URL. each time it is going to put new URl inside DOM.
<br><br>

</body>
</html>

